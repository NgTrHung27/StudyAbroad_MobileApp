// ignore_for_file: constant_identifier_names

enum Gender {
  Male,
  Female,
}

enum DegreeType {
  Highschool,
  University,
}

enum GradeType {
  GPA,
  CGPA,
}

enum CertificateType {
  IELTS,
  TOEFL,
}

enum UserRole {
  ADMIN,
  Student,
}

enum StudentStatus {
  STUDYING,
  DROPPED,
  AWAITING,
  APPROVED,
}

enum TitleForm {
  FEEDBACK,
  SYSTEM,
  REFUND,
  BILLING,
  SUBSCRIPTION,
  SCHOLARSHIP,
  PROCEDURE
}
